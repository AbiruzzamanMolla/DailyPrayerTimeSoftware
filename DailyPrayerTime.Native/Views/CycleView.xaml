<UserControl x:Class="DailyPrayerTime.Native.Views.CycleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="350"
             Background="Transparent">

    <Grid Margin="15,0,15,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="🌙" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock Grid.Column="0" Text="{DynamicResource Cycle_Title}" FontWeight="Bold" FontSize="14" Foreground="White" VerticalAlignment="Center" Margin="28,0,0,0"/>
            <Button Grid.Column="2" Content="⟳" Background="Transparent" BorderBrush="Transparent" Foreground="White" FontSize="16" Cursor="Hand" Click="Refresh_Click" VerticalAlignment="Center" Padding="4"/>
        </Grid>

        <!-- Status Card -->
        <Border Grid.Row="1" x:Name="StatusCard" Background="#1AFFFFFF" BorderBrush="#2AFFFFFF" BorderThickness="1" CornerRadius="12" Padding="15,12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,15,0">
                    <TextBlock x:Name="StatusEmoji" Text="🌙" FontSize="28" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusText" Text="{DynamicResource Cycle_Unknown}" FontSize="14" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="StatusDetail" Text="" FontSize="11" Foreground="White" Opacity="0.6" Margin="0,2,0,0"/>
                    <TextBlock x:Name="StatusPrediction" Text="" FontSize="10" Foreground="#fbbf24" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Quick Actions -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="StartPeriodBtn" Grid.Column="0" Content="{DynamicResource Cycle_StartPeriod}" Background="#dc2626" Foreground="White" FontSize="11" FontWeight="SemiBold" Padding="10,8" Click="StartPeriod_Click" Cursor="Hand" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button x:Name="EndPeriodBtn" Grid.Column="2" Content="{DynamicResource Cycle_EndPeriod}" Background="#059669" Foreground="White" FontSize="11" FontWeight="SemiBold" Padding="10,8" Click="EndPeriod_Click" Cursor="Hand" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <!-- Stats Row -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#1AFFFFFF" CornerRadius="8" Padding="8,6">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="AvgCycleText" Text="--" FontSize="16" FontWeight="Bold" Foreground="#60a5fa" HorizontalAlignment="Center"/>
                    <TextBlock Text="Cycle" FontSize="8" Foreground="White" Opacity="0.5" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#1AFFFFFF" CornerRadius="8" Padding="8,6">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="AvgPeriodText" Text="--" FontSize="16" FontWeight="Bold" Foreground="#f87171" HorizontalAlignment="Center"/>
                    <TextBlock Text="Period" FontSize="8" Foreground="White" Opacity="0.5" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="4" Background="#1AFFFFFF" CornerRadius="8" Padding="8,6">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="NextPeriodText" Text="--" FontSize="16" FontWeight="Bold" Foreground="#fbbf24" HorizontalAlignment="Center"/>
                    <TextBlock Text="Until Next" FontSize="8" Foreground="White" Opacity="0.5" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Recent Entries -->
        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="EntriesList">
                <TextBlock x:Name="EmptyText" Text="No entries yet. Tap 'Start Period' to begin tracking." Foreground="White" Opacity="0.5" FontSize="12" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
