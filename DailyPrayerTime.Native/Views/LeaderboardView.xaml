<UserControl x:Class="DailyPrayerTime.Native.Views.LeaderboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="350"
             Background="Transparent">

    <Grid Margin="15,0,15,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{DynamicResource Leaderboard_Title}" FontWeight="Bold" FontSize="13" Foreground="White" Opacity="0.8" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="MonthLabel" Text="" FontSize="10" Foreground="White" Opacity="0.5" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Button Grid.Column="2" Content="{DynamicResource Leaderboard_Refresh}" Background="Transparent" BorderBrush="#33FFFFFF" BorderThickness="1" Foreground="White" Padding="10,4" CornerRadius="6" Cursor="Hand" Click="Refresh_Click" FontSize="10"/>
        </Grid>

        <!-- My Ranking Card -->
        <Border Grid.Row="1" Background="#1AFFFFFF" BorderBrush="#2AFFFFFF" BorderThickness="1" CornerRadius="10" Padding="12,10" Margin="0,0,0,10" x:Name="MyRankCard" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="MyRankText" FontSize="22" FontWeight="Bold" Foreground="#34d399" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="MyNameText" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock x:Name="MyStatsText" FontSize="10" Foreground="White" Opacity="0.6"/>
                </StackPanel>
                <TextBlock Grid.Column="2" x:Name="MyRateText" FontSize="16" FontWeight="Bold" Foreground="#fbbf24" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Leaderboard List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="LeaderboardList">
                <TextBlock x:Name="LoadingText" Text="{DynamicResource Leaderboard_Loading}" Foreground="White" Opacity="0.5" FontSize="12" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
