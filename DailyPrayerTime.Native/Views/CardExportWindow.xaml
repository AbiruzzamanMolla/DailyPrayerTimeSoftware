<Window x:Class="DailyPrayerTime.Native.Views.CardExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource CardExport_Title}"
        Height="420" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="#064E3B" CornerRadius="16" BorderBrush="#2AFFFFFF" BorderThickness="1.5">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,5,0,20">
                <TextBlock Text="🕌" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="{DynamicResource CardExport_Title}" FontSize="18" FontWeight="Bold" Foreground="#FBBF24" HorizontalAlignment="Center"/>
                <TextBlock Text="{DynamicResource CardExport_SubText}" FontSize="12" Foreground="White" Opacity="0.7" HorizontalAlignment="Center" Margin="0,4,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>

            <!-- Selection Body -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="10,0,10,0">
                <StackPanel.Resources>
                    <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                        <Border x:Name="Border" CornerRadius="8" Background="#022C22" BorderBrush="#33FFFFFF" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="32" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="Transparent" />
                                <Path x:Name="Arrow" Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#065F46" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#022C22" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>

                    <ControlTemplate x:Key="ComboBoxTemplate" TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Template="{StaticResource ComboBoxToggleButton}" 
                                          Grid.Column="2" 
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press" />
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                              Margin="12,8,36,8" 
                                              HorizontalAlignment="Left" 
                                              VerticalAlignment="Center" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="#022C22" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="8" Margin="0,4,0,0">
                                        <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </StackPanel.Resources>

                <!-- Card Type Selection -->
                <TextBlock Text="{DynamicResource CardExport_SelectReportType}" FontSize="11" FontWeight="Bold" Foreground="White" Opacity="0.5" Margin="0,0,0,10"/>
                
                <Grid Margin="0,0,0,25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <RadioButton x:Name="RadioWeekly" Grid.Column="0" Content="{DynamicResource CardExport_WeeklyReport}" Foreground="White" FontSize="14" FontWeight="SemiBold" IsChecked="True" Checked="ReportType_Changed" Cursor="Hand"/>
                    <RadioButton x:Name="RadioMonthly" Grid.Column="1" Content="{DynamicResource CardExport_MonthlyReport}" Foreground="White" FontSize="14" FontWeight="SemiBold" Checked="ReportType_Changed" Cursor="Hand"/>
                </Grid>

                <!-- Period Selection -->
                <TextBlock Text="{DynamicResource CardExport_SelectPeriod}" FontSize="11" FontWeight="Bold" Foreground="White" Opacity="0.5" Margin="0,0,0,8"/>
                <ComboBox x:Name="PeriodComboBox" Template="{StaticResource ComboBoxTemplate}" Foreground="White" FontSize="13" Cursor="Hand">
                    <ComboBox.Resources>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="#022C22"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border Name="Border" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Border" Property="Background" Value="#10B981" />
                                                <Setter Property="Foreground" Value="White" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Resources>
                </ComboBox>
            </StackPanel>

            <!-- Actions Footer -->
            <Grid Grid.Row="2" Margin="0,15,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="{DynamicResource Cancel}" Grid.Column="0" Background="Transparent" BorderBrush="#33FFFFFF" BorderThickness="1" Foreground="White" Padding="0,12" Margin="0,0,8,0" Click="Cancel_Click" Cursor="Hand" FontSize="13" FontWeight="SemiBold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="{DynamicResource CardExport_Generate}" Grid.Column="1" Background="#10B981" BorderThickness="0" Foreground="White" Padding="0,12" Margin="8,0,0,0" Click="Generate_Click" Cursor="Hand" FontSize="13" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
