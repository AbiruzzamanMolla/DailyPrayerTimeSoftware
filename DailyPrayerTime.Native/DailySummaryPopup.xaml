<Window x:Class="DailyPrayerTime.Native.DailySummaryPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Daily Summary" Height="500" Width="450"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="CenterScreen" ShowInTaskbar="False">
    <Window.Resources>
        <LinearGradientBrush x:Key="MainGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#064E3B" Offset="0"/>
            <GradientStop Color="#022C22" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    
    <Border CornerRadius="20" Margin="10" Background="{StaticResource MainGradient}" BorderBrush="#30FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="1" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel Margin="20">
            <TextBlock Text="DAILY SUMMARY REVIEW" Foreground="#FBBF24" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Review your deeds before the day ends" Foreground="White" Opacity="0.7" FontSize="11" HorizontalAlignment="Center" Margin="0,5,0,15"/>

            <ScrollViewer Height="280">
                <ItemsControl x:Name="SummaryList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#1AFFFFFF" CornerRadius="10" Padding="12" Margin="0,0,0,8">
                                <Grid>
                                    <TextBlock Text="{Binding Label}" Foreground="White" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding StatusText}" Foreground="#34d399" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Grid Margin="0,20,0,0">
                <Button Content="FINALIZE DAY" Click="Finalize_Click" HorizontalAlignment="Center" Padding="40,12" Background="#34d399" Foreground="#022c22" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                             <Setter Property="CornerRadius" Value="15"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </StackPanel>
    </Border>
</Window>
