<Window x:Class="DailyPrayerTime.Native.DeedPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prayer Deed Tracking" Height="300" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="CenterScreen" ShowInTaskbar="False">
    
    <Border CornerRadius="20" Margin="10" Background="#170939" BorderBrush="#30FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="1" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel Margin="20">
            <TextBlock x:Name="TitleText" Text="DID YOU PRAY?" Foreground="#FBBF24" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Mark your deeds for this prayer" Foreground="White" Opacity="0.7" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,15"/>
            
            <ItemsControl x:Name="DeedCheckList" Margin="0,10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Label}" IsChecked="{Binding IsChecked}" Foreground="White" FontSize="14" Margin="10,5">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox">
                                    <Setter Property="Padding" Value="6,0,0,0"/>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid Margin="0,20,0,0">
                <Button Content="SAVE &amp; CLOSE" Click="Save_Click" HorizontalAlignment="Center" Padding="30,10" Background="#FBBF24" Foreground="#170939" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                             <Setter Property="CornerRadius" Value="15"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </StackPanel>
    </Border>
</Window>
