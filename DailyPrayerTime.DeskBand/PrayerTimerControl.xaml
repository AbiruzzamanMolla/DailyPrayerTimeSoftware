<UserControl x:Class="DailyPrayerTime.DeskBand.PrayerTimerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid Background="Transparent">
        <Border x:Name="BackgroundBorder" Background="#00000000" CornerRadius="2">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                <!-- Status indicator -->
                <Border x:Name="AccentBar" Width="3" Height="14" Background="#10b981" CornerRadius="1" Margin="0,0,8,0"/>
                
                <!-- Prayer Label -->
                <TextBlock x:Name="NameText" Text="Fajr ends in:" Foreground="#A0A0A0" FontSize="11" VerticalAlignment="Center" Margin="0,0,5,0" />
                
                <!-- Countdown -->
                <TextBlock x:Name="CountdownText" Text="00:00:00" Foreground="#2ECC71" FontSize="12" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center" />
                
                <!-- Context Menu Hint / Dropdown -->
                <Path Data="M0,0 L4,4 L8,0" Stroke="#44FFFFFF" StrokeThickness="1" Margin="8,2,0,0" VerticalAlignment="Center" Opacity="0.5"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
